<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DAA"
        tools:targetApi="34">
        <!-- Part 1
        android:name="ch.heigvd.iict.daa.lab2.activity.MainActivityWelcome"
        -->
        <!-- Part 3
        android:name="ch.heigvd.iict.daa.lab2.MainActivityFragment1"
        -->
        <!-- Part 2
        android:name="ch.heigvd.iict.daa.lab2.MainActivityFragment2"
        -->
        <activity
            android:name="ch.heigvd.iict.daa.lab2.MainActivityFragment1"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Part 1
            <intent-filter>
               <action android:name="android.intent.action.MAIN" />
               <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <!-- Part 1
        <activity android:name=".activity.MainActivityEdit" />
        -->
    </application>
</manifest>